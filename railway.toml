[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npx prisma generate"

[deploy]
startCommand = "cd backend && npx prisma db push && node src/seed.js && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
