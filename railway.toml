[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npx prisma generate"

[deploy]
startCommand = "node backend/start.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
