[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npx prisma generate && npx prisma db push && node src/seed.js"

[deploy]
startCommand = "cd backend && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
