{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /c/Users/Hesta/pos-system && git add -A && git diff --cached --quiet || git commit -m \"auto-save: $(date +%Y-%m-%d_%H:%M:%S)\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
